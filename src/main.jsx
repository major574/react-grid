var React = require('react');
var ReactDOM = require('react-dom');
var BoxManager = require('./components/BoxManager.jsx');

ReactDOM.render(<BoxManager title="ingredients"/>, document.getElementById('aBox'));
