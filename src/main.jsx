var React = require('react');
var ReactDOM = require('react-dom');
var BoxManager = require('./components/BoxManager.jsx');

ReactDOM.render(<BoxManager title="A Box"/>, document.getElementById('aBox'));
